# Install locally

```bash
npm install
```

# Usage

## 启动服务
```bash
node index.js
```
## 或指定端口和绑定地址

```bash
node index.js --port=9000 --host=127.0.0.1
```
##  浏览器打开
默认 http://<你的IP>:8800  
默认绑定 0.0.0.0，局域网内可直接通过 <host_ip>:8800 访问。


